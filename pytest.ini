[pytest]
testpaths = tests
addopts = -q
markers =
    network: test makes real outbound API calls (deselect with -m "not network")
    integration: live end-to-end tests that hit real APIs + DB (opt in with RUN_INTEGRATION=1)
